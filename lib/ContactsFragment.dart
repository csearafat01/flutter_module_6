import 'package:flutter/cupertino.dart';

class ContactsFragment extends StatelessWidget {
  const ContactsFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ContactsFragment'),
    );
  }
}
