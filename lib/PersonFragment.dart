import 'package:flutter/cupertino.dart';

class PersonFragment extends StatelessWidget {
  const PersonFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('PersonFragment'),
    );
  }
}
